services:
  # === API ===================================================================
  kudab-api:
    restart: always
    env_file:
      - ./services/kudab-api/.env
    environment:
      APP_ENV: production
      APP_DEBUG: "false"   # чтобы About не показывал ENABLED

  # Планировщик для API (если не нужен — удали сервис)
  kudab-api-scheduler:
    image: ${API_IMAGE:-kudab-api}
    command: sh -lc 'while true; do php artisan schedule:run --no-interaction; sleep 60; done'
    restart: always
    env_file:
      - ./services/kudab-api/.env
    environment:
      APP_ENV: production
    depends_on:
      - kudab-db
      - redis

  # === PARSER + QUEUE ========================================================
  kudab-parser:
    restart: always
    env_file:
      - ./services/kudab-parser/.env
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      # фиксим tinker/psysh "Writing to /.config/psysh is not allowed"
      XDG_CONFIG_HOME: /tmp
      HOME: /tmp
      # дублируем БД/Redis — чтобы НЕ свалился на sqlite
      DB_CONNECTION: pgsql
      DB_HOST: kudab-db
      DB_PORT: 5432
      DB_DATABASE: ${POSTGRES_DB:-kudab}
      DB_USERNAME: ${POSTGRES_USER:-kudab}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-kudab}
      REDIS_CLIENT: phpredis
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redispass}
    depends_on:
      - kudab-db
      - redis

  # Horizon (воркеры очередей парсера)
  kudab-horizon:
    restart: always
    env_file:
      - ./services/kudab-parser/.env
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      XDG_CONFIG_HOME: /tmp
      HOME: /tmp
      QUEUE_CONNECTION: redis
      HORIZON_PREFIX: "kudab_prod_hz:"
      REDIS_CLIENT: phpredis
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redispass}
      # те же БД-параметры на всякий
      DB_CONNECTION: pgsql
      DB_HOST: kudab-db
      DB_PORT: 5432
      DB_DATABASE: ${POSTGRES_DB:-kudab}
      DB_USERNAME: ${POSTGRES_USER:-kudab}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-kudab}
    command: sh -lc "php artisan optimize:clear && php artisan horizon"
    depends_on:
      - redis
      - kudab-db

  # Планировщик задач парсера (каждую минуту)
  kudab-parser-scheduler:
    image: ${PARSER_IMAGE:-kudab-parser}
    command: sh -lc 'while true; do php artisan schedule:run --no-interaction; sleep 60; done'
    restart: always
    env_file:
      - ./services/kudab-parser/.env
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      XDG_CONFIG_HOME: /tmp
      HOME: /tmp
      # жёстко дублируем БД/Redis — чтобы НИКОГДА не свалился на sqlite
      DB_CONNECTION: pgsql
      DB_HOST: kudab-db
      DB_PORT: 5432
      DB_DATABASE: ${POSTGRES_DB:-kudab}
      DB_USERNAME: ${POSTGRES_USER:-kudab}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-kudab}
      REDIS_CLIENT: phpredis
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redispass}
    depends_on:
      - kudab-horizon
      - kudab-db
      - redis

  # === FRONT/BOT/NGINX ======================================================
  kudab-frontend:
    restart: always
    environment:
      NODE_ENV: production

  kudab-bot:
    env_file:
      - ./services/kudab-bot/.env
    environment:
      BOT_ENV: production
      BOT_MODE: webhook
      RUNNER: gunicorn
    restart: unless-stopped

  kudab-nginx:
    restart: always
    environment:
      NGINX_ENV: production
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./services/kudab-nginx/default.prod.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certs/prod:/etc/nginx/certs:ro

  # === DB ====================================================================
  kudab-db:
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-kudab}
      POSTGRES_USER: ${POSTGRES_USER:-kudab}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-kudab}

volumes:
  pgdata:

