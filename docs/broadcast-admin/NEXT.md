# Рассылка: что дальше

Составлено 2026-09-14 по коду на 2026-09-09 18:23 и по живому стенду.
Состояние — `PLAN.md`, расхождения интерфейса — `UI-FIXES.md`, каденс ленты и
посты о площадках — `CADENCE.md` (там же числа, на которых держится выбор).

Порядок не произвольный. Сначала то, без чего включённый канал молчит или
стыдно постит, потом новые возможности, потом интерфейсные хвосты. Каждая
очередь оставляет систему рабочей — это важно, потому что деплой ручной.

---

## Очередь 0. Перед тем как включать канал

Восемь работ, все мелкие или средние. Без них включение даёт либо тишину без
единой ошибки, либо анонс вчерашнего события.

### 0.0. Запушить и решить судьбу ветки ✅ запушено 2026-09-14

27 коммитов двух дней существуют только на одной машине: infra 9,
kudab-admin 8, kudab-api 7, kudab-bot 3. Бэкенд рассылки целиком сидит на ветке
`feat/venue-page-rebuild` — ветке от страницы места.

Порядок пуша строго: **подмодули → инфра**. Деплой идёт по SHA-пинам, и при
обратном порядке `prod-pull` упадёт на коммитах, которых нет на GitHub.
Автодеплой выключен вручную с 26.01.2026 (`gh api … workflows` →
`state: disabled_manually`), так что пуш прод не тронет.

### 0.1. Снять состояние прода (только чтение)

Все выводы ниже сделаны по стенду. С прода нужны четыре числа:

- сколько каналов `enabled`, у каких есть владелец и город;
- сколько открытых записей с `publish_at IS NULL` (их доставит первое же окно);
- есть ли у каналов ключ `feed_limit` в `settings` — **без него канал при
  выкатке перейдёт с «одного поста в полёте» на семь**
  (`TelegramChatBroadcast.php:56-63`);
- какой код там выкачен (`git rev-parse HEAD`, `git submodule status`).

### 0.2. Канал без владельца молчит — сделать это видимым ✅ сделано 2026-09-14

`TelegramChatBroadcastService.php:583` — `if (! $ownerTelegramId) { continue; }`.
Ни поста, ни лога, ни ЛС-сигнала (`:1458`), ни признака в админке. А привязка из
админки владельца **намеренно не пишет** (`AdminBroadcastController.php:978`:
«за веб-админом нет телеграм-пользователя»). То есть канал, привязанный формой,
включается и молчит.

- `channelPayload` (`:1121-1153`) — отдавать владельца.
- `channelProblems` (`:1180-1218`) — `danger` «нет владельца, канал не публикует».
- `updateChannel` — отказ на `enabled=true` без владельца.
- `:583` — заменить немой `continue` на `Log::warning`.

**Решено и сделано:** канал без телеграм-владельца включать нельзя.
`channelPayload` отдаёт `has_owner`, `channelProblems` — danger, `updateChannel`
отвечает 422, пропуск в поллере пишется в лог (`broadcast.poll.skipped_no_owner`),
тумблер автопостинга в админке заблокирован с объяснением.
Тест: `tests/Feature/Api/AdminBroadcastOwnerTest.php`.

### 0.3. Часовой пояс окна выпуска ✅ сделано 2026-09-14

Лента планирует в МСК (`publish_at` = 07:00+00 под подписью «10:00»), а окно
считается в UTC: `TelegramChatBroadcastService.php:1541` делает `setTime($hour)`
от `Carbon::now()` при `config/app.php:68 'timezone' => 'UTC'`. **`daily_10`
открывается в 13:00 МСК.**

Править `isSingleRunDue` (оба вызова — `:502` доставка и `:643` наполнение),
приводя `$candidate` к `Europe/Moscow`. Глобальный `config/app.php` не трогать.
Заодно проверить `periodWindowHours` (`:1416`).

Это надо сделать **до** слотов: иначе слоты закрепят ошибку, и «вечерний пост в
19:00» будет непроверяем.

**Сделано:** константа `SCHEDULE_TZ` и приведение окна к Москве в обеих ветках
(`daily_`, `weekly_`). Тест `test_daily_window_opens_by_moscow_hour` берёт
границу 09:30/10:30 МСК — на старом коде падает. Осталась соседняя правка из
очереди 3: сетка админки по-прежнему считает день в поясе браузера.

### 0.4. Guard на отправке: событие кончилось и текст из прошлого

Между постановкой и отправкой проверки времени нет вообще. Два разных случая:

- **Событие закончилось.** Грузить модель и сравнивать `end_time ?: start_time`
  с `now` — то же правило, что уже записано в админке (`:305-314`, `:621-632`) и
  в подборе (`:943-952`). Не `start_time`, иначе снесутся живые многодневки.
  Снятую запись помечать `skipped` с причиной, по образцу уже сделанного снятия
  удалённого события.
- **Текст собран под другой день.** `ensureEventCaption` (`:1218-1222`) выходит,
  если `caption` непустой, — уйдёт дословно «сегодня, 17:00» про прошлую среду.
  Либо пересобирать шаблонный текст, когда день отправки не совпал с днём
  сборки, либо снимать запись. **Ручной текст (`caption_source = manual`) не
  трогать.**

На стенде это не гипотеза: из шести открытых записей канала Воронежа пять
просрочены, у двух события уже прошли, у трёх события живы, но в тексте даты
из прошлого.

### 0.5. Зазор между постами и портрет вне расписания

Портрет площадки уходит мимо гейта расписания (`:502`), через ~2 минуты после
постановки — подтверждено живыми отправками 16/23/30 июля. То есть «один пост в
день» на канале не соблюдался и раньше.

- Минимальный зазор считать по `max(posted_at)` записей канала, **не** по
  `last_run_at`: его двигает не всякая отправка.
- Отдельно: поллер читает **одну** запись на канал за тик
  (`TelegramChatBroadcastItemRepository.php:356` → `first()`), и запись,
  заблокированная расписанием, останавливает канал целиком, включая портрет с
  независимым каденсом. Выбирать по одной записи каждого вида.

### 0.6. Разобрать очередь руками

В очереди канала 1 — шесть событийных записей и портрет. Две записи снять
(события прошли), трём поменять день (события живы, но `scopeUpcoming` заново
их не подберёт: `start_time` в прошлом), портрет оставить. Делать **после**
0.4, иначе почистим руками то, что должен ловить код, и не узнаем, ловит ли.

### 0.7. Бейдж «событие уже прошло» врёт

`index.vue:692-701` сравнивает конец события с назначенным `publish_at`, а у
просроченной записи он сам в прошлом. Сравнивать с моментом реальной отправки:
`const effective = pub > now ? pub : now`. Две строки, но без них владелец не
увидит глазами то, что чинит 0.4.

### 0.8. Закрыть `PATCH /items/{id}`

Пятый писатель `publish_at` (`:461-483`), и в нём нет ни вытеснения занявшего
день, ни запрета «пост после события» — обе проверки есть в `enqueue` и `move`.
Из карточки правки уже сегодня можно поставить два поста на один день. Чинить
**до** слотов: под слоты этот путь обойдёт ключ слота.

---

## Очередь 1. Город канала из админки

То, что просил владелец. Работа мелкая — почти всё уже есть.

1. **`channelPayload`** — вернуть `city_id` и название города (сейчас города нет
   в ответе вовсе, единственный след — текст проблемы `:1187`).
2. **`updateChannel`** (`:1004-1009`) — добавить `city_id` в белый список
   валидации и писать через **`TelegramChatService::forceSetChatCity`**
   (`:129`). Не через `$chat->update()`: `city_id` **вне `$fillable`**
   (`TelegramChat.php:14-23`), наивное присваивание вернёт 200 и не изменит
   ничего.
3. **Список городов** — ручка уже есть: `GET /api/admin/select/cities`
   (`routes/api.php:55`). Но она **не фильтрует статус**, а `resolveCity` берёт
   только `status='active'` — сейчас это 3 города из 14. Либо добавить фильтр
   параметром, либо отдавать города в `meta` вместе с каналами, как уже сделано
   для шаблонов (`AdminBroadcastController.php:69-80` → `settings.vue:21`).
4. **`settings.vue`** — селект города в карточке настроек плюс город в
   подзаголовке, как в макете.
5. **Тест** на `updateChannel` с городом. В фикстурах город нельзя создать через
   `City::create`: `location` — PostGIS-колонка `NOT NULL` без дефолта.

**Ловушки.**
- Справочник удалит город → `ON DELETE SET NULL` молча обнулит его у канала, и
  канал перестанет постить без единой ошибки. Стоит добавить это в
  `channelProblems`.
- Смена города у канала с набранной лентой не чистит чужие записи, а админская
  постановка вообще не проверяет город события (в отличие от старого ботовского
  флоу). После смены города — предлагать пересборку.

---

## Очередь 2. Подключение каналов и сервисные боты

### Честный ответ про «определить автоматически»

**Перечислить каналы, где бот админ, нельзя.** В Bot API нет метода, который
возвращает список чатов бота: `getChat`, `getChatMember`,
`getChatAdministrators` — все требуют уже известный идентификатор. `getUpdates`
отдаёт события, а не состояние. `my_chat_member` приходит **один раз**, в момент
изменения статуса, поэтому канал, где бот админ с прошлого года, сам не
проявится — это и есть корень вопроса.

Чего делать **не** стоит: считать признаком подключения `getChat` — он отвечает
успехом для любого публичного канала, где бот посторонний (проверено живьём на
`@durov`). Признак только один: `getChatMember` со `status` в
`{administrator, creator}` — это уже реализовано.

### Что уже работает и чего не хватает

Автообнаружение по факту **есть**: когда бота делают админом, `my_chat_member`
пишет чат в `telegram.chats` вместе с владельцем и названием
(`channels.py:26-100`). Но **строки рассылки при этом не создаётся**
(`getOrCreateByChatId` на этом пути не зовётся), а админский список строится по
`chat_broadcasts` — поэтому канал в админке не появляется. Текст на
`settings.vue:321` обещает обратное.

**Первый шаг — одна строка:** вызвать `getOrCreateByChatId` на пути привязки из
бота. Сценарий «добавил бота админом в канал → канал виден в админке» начинает
работать, и это закрывает большую часть запроса.

### Дальше — пикер вместо ручного ввода

Telegram даёт ровно одну штатную замену перечислению: кнопка
`KeyboardButton(request_chat=KeyboardButtonRequestChat(chat_is_channel=True,
bot_is_member=True, bot_administrator_rights=…))`. Список каналов строит **сам
клиент владельца**, работает и для приватных, боту приходит `message.chat_shared`
с `chat_id`, `title`, `username` (поля проверены интроспекцией aiogram 3.12 в
контейнере).

Работа: кнопка «Подключить канал через бота» в админке → deep-link
`t.me/<bot>?start=run__link` → новый хендлер в боте → внутренняя ручка API →
общее действие привязки (вынести из `linkChannel`, чтобы путь был один).

**Обязательно:** deep-link-диспетчер роль сейчас не проверяет вовсе, а
bot-путь привязки открыт роли `user` — то есть любому, кто нажимал `/start`.
Payload должен нести одноразовый токен, выданный админкой конкретному админу.
Иначе привязка понижается с «админ сайта» до «любой пользователь бота».

### «Кто этот бот и где он админ» — в админку

Две внутренние ручки в боте: `/internal/bot-info` (`get_me`, режим, жив ли
поллинг, задан ли токен — **без самого токена**) и `/internal/chats-status`
(по каждому известному `chat_id` — `getChatMember` со статусом и правом
публикации). В админке дополнить существующий блок «Что остаётся за ботом», а у
каждого канала в списке показать «бот админ / бот не админ».

`/diag` для этого не переиспользовать: он без авторизации.

### Страховка: кандидаты из `channel_post`

Любой пост из канала, где бот состоит, приносит его `chat_id`, и `channel_post`
уже в `allowed_updates`. Незнакомый `chat_id` писать кандидатом и показывать
«Замечен канал — подключить?». Метка кандидата — **только отдельная колонка**:
`is_active=false` уже означает «бота выгнали». Полного списка это не даёт —
молчащий канал не проявится, — поэтому как страховка, не вместо пикера.

### Мультибот и токены в админке — отвергнуто, и вот почему

**Токены ботов в базе:** локальная база — дамп прода, значит боевой токен
приедет на ноутбук при ближайшем импорте; при другом `APP_KEY` — исключение при
каждом чтении. И смена токена всё равно не подхватится: настройки кэшируются,
`Bot` — глобальный синглтон, поллер строит его до цикла. Пришлось бы
перезапускать процесс — то есть выигрыша против env нет.

**Несколько ботов (таблица ботов, у канала ссылка на бота):** потребность
нулевая — три канала, все на одном боте. Цена высокая: каждый постящий бот
обязан поллиться, иначе не получит `my_chat_member`; 27 команд ЛС размножатся
на каждого бота; привязка без `bot_code` начнёт уверенно врать про права.
Вернуться к этому стоит, только если появится канал, который **должен** вести
другой бот.

**MTProto/Telethon (перечисление диалогов user-аккаунтом):** документация прямо
говорит «Only users can use this method» — на сервере пришлось бы держать живую
сессию личного аккаунта владельца. Цена несопоставима с экономией на
подключении трёх каналов.

**Что остаётся из «настроить бота из админки»:** показывать состояние (кто бот,
жив ли, где админ) — да, полезно и дёшево. Хранить и менять токены — нет.

---

## Очередь 3. Несколько постов в один день

> **Уточнено 2026-09-14 замерами — см. `CADENCE.md`.** Два слота 10:00 и 19:00
> МСК подтвердились: неделя из 14 постов занимает 27 сетей из 33 доступных,
> запас есть. Третий слот арифметически невозможен — 21 пост требует 41 сети, а
> на седьмом дне ленты их всего 19, и слой анти-однообразия при нехватке молча
> берёт неотфильтрованный пул. Зазор 90 минут подтверждён, **но обязан
> выкатываться вместе с отсечкой просрочки** (запись старше 2 часов —
> снимается), иначе пять просроченных записей растянутся каплей на шесть часов.
> Считать зазор по `max(posted_at)` любого типа: расхождение с `last_run_at` на
> живых данных — 4 часа 08 минут.

### Решение: фиксированные слоты дня

В `settings` канала рядом с `period` появляется ключ `slots` — список часов в
Europe/Moscow, например `[10, 19]`. Пустой `slots` = ровно нынешнее поведение,
один слот из `period`, поэтому выкат ничего не меняет молча. Ключом слота
становится момент (день + час слота в МСК).

**Почему так, а не иначе.**
- *Свободное время у каждого поста* уничтожает опоры интерфейса: исчезает
  понятие «пустой слот», на котором держатся предложения в пустом дне, счётчик
  «заполнено N из 7» и всё перетаскивание. И заставляет владельца назначать
  время руками каждому посту.
- *Очередь внутри дня (`position`, все посты с одинаковым `publish_at`)* —
  единственный вариант, требующий миграции, и он навсегда закрепляет
  расхождение «10:00 в сетке против 13:00 в канале»: время поста перестаёт быть
  выразимым.
- *Растянуть горизонт вместо плотности* — другая задача: на 30-й день ставить
  просто нечего, подбор смотрит на 14 дней вперёд.
- *Снять `UNIQUE(broadcast_id, event_id)`* — соблазн мимо цели: индекс про
  «одно событие дважды в канале», а не про «два поста в день». Несколько постов
  в день — это разные события, индекс им не мешает; зато на нём висит
  анти-дубль первого слоя.

Данные менять не надо: `publish_at` — `timestamp(0) with time zone`, время в нём
уже хранится; `settings` — json, сеттеры модели сливают ключи.

### Работы

1. **Ключ слота — в шести путях записи `publish_at`**, а не в двух. Сейчас
   вытеснение есть только в `enqueue` и `move`; `update` и `publishNow` пишут без
   проверок; `fillFeedDays` назначает день; `enqueueDueForAllChannels` **день не
   назначает вовсе** — ему темп задаёт `isSingleRunDue`. Вынести один приватный
   `resolveSlotOccupant(broadcast, publishAt, excludeItemId)`, ищущий занявшего
   **запросом** по окну слота, а не перебором всей ленты.
2. **Отправка идёт по собственному моменту поста.** Запись с `publish_at` уже
   отфильтрована на «пора» — `isSingleRunDue` к ней не применяется. Запись
   **без** `publish_at` сохраняет прежнее поведение (событийная — под суточным
   окном, портрет — мимо него). Формулировать именно так: портрет как раз без дня.
3. **Наполнитель тоже должен знать про слоты**, иначе лента расходуется вдвое
   быстрее, чем наполняется.
4. **Разделить `feed_limit`** на `horizon_days` (сколько дней вперёд крутить) и
   кап записей: сейчас одно число значит обе вещи, и при двух слотах смыслы
   расходятся вдвое.
5. **Сетка админки: день как контейнер слотов.** Ключ слота — `день+час`,
   «не больше одного поста в слоте» сохраняется, поэтому все 48 обращений к
   `slot.item` в шаблоне остаются валидными. **Предварительно** перевести сетку
   на Europe/Moscow: сейчас день и час считаются в поясе браузера
   (`index.vue:420-465`), и у админа не из Москвы ключ слота разойдётся с
   серверным.
6. **Анти-дубли, слой площадок** — с «была/не была» на «не больше N раз в окне».
   При 14 постах в неделю на 34 площадки фильтр «площадка уже в ленте» вырежет
   почти всё и молча отключится (при пустом результате берётся неотфильтрованный
   пул). Слои 1 и 2 переход переживают без правок.
7. **Сигнал о простое** — `periodWindowHours` жёстко считает 24 ч для любого
   `daily_*`; при двух слотах нормальное молчание вдвое короче. Правило
   продублировано в `channelPayload` — свести в одно место.
8. **Тесты** (живьём отправку на стенде не проверить): три поста на один день
   выходят в порядке слотов и с зазором, а не пачкой; запись с `publish_at` в
   будущем не выдаётся; запись без `publish_at` по-прежнему под суточным окном.
9. **Перезамерить пул.** Замер «138 кандидатов, 93 заголовка» делался под семь
   постов и при капе 100; сейчас кап 400, а два слота — это 14 постов в неделю.

**Рекомендация: начинать с двух слотов.** Три слота — 21 пост в неделю на 34
площадки, и слой анти-дублей там выродится в «фильтра нет».

---

## Очередь 3б. Посты о площадках

Полный разбор — в `CADENCE.md`, здесь только суть и порядок.

Портрет площадки перестаёт быть особым видом поста: **день и час назначаются
при постановке, а гейт доставки спрашивает про назначенный момент, а не про
тип записи**. Сегодня `publish_at` портрету не ставит ни один путь кода (0 из 8
записей), поэтому для него сделано исключение в доставке — и он уезжает первым
же тиком в произвольный час, вплотную за дневным событием.

**Каденс — один портрет в неделю.** Пул: 35 площадок Воронежа с готовым
текстом из 110 активных, из них 18 с ближайшим событием. При кулдауне 90 дней
потолок частоты = пул ÷ 12,86, то есть 2,7 формально и **1,4 по качественным**.

- **Раньше и отдельно:** снять автопостановку портретов с расписания на время
  работ (она не проверяет `BroadcastSafety` и кладёт записи в очередь боевого
  канала со стенда); пометка ошибкой в venue-ветке бота; имя площадки в выдаче
  ленты; память об отклонённых в ротации; догенерация недостающих портретов.
- **Одной работой со слотами:** `publish_at` при постановке, гейт по моменту,
  ключ слота, зазор, сетка.
- **Позже:** админские ручки (пул площадок и постановка), портрет в
  предложениях пустого слота, правка текста и картинок — последняя только
  после трёх правок в обработчике, иначе первая же правка вешает канал.

## Очередь 4. Интерфейсные хвосты

Порядок такой, потому что каждый бэкендный шаг закрывает сразу два пункта
макета. Подробности — `UI-FIXES.md`.

1. **Отдать `chain` в payload ленты** → «Разбавить» + «N-й за неделю».
2. **Отдать интересы события** в `suggestions` и `feed` → фильтр по теме,
   колонка «Чем занята неделя», причина «темы ещё не было на неделе».
3. **Счётчик символов и лимит 1024** в карточке правки — сейчас правка
   принимает 4096, и подпись длиннее 1024 молча отправит пост **без картинок**.
4. **«Вернуть в пул»** — отклонение сейчас необратимо из интерфейса: случайное
   нажатие прячет событие на 30 дней, и вернуть его нечем.
5. Мелочи: время в превью, бейдж «обложка», «следующая публикация через N ч»,
   «Показать все N» для свёрнутой сети, «Выбрать самому» в пустом слоте.
6. **Портрет площадки как предложение в пустом слоте** — это же и есть
   невыполненное решение «портреты отдельным слотом»: ставить venue-записи
   `publish_at` и показывать их в сетке.

---

## Очередь 5. Уборка и долги

- **Тесты на контроллер**: `enqueue` и `rebuild` первыми — там больше всего
  ветвлений.
- **Пересборка не бережёт ручной текст**, хотя докблок обещает: добавить фильтр
  по `caption_source` либо поправить докблок.
- **Кнопка «📢 В чат»** всё ещё рисуется на двух карточках бота; обработчик уже
  заглушен. Убрать кнопку, заглушку оставить ради старых сообщений.
- **~130 строк мёртвого кода** после `return` в `broadcast.py`.
- **`my_chat_member` зарегистрирован дважды**; побеждает `chat_members.py`, а в
  `channels.py` лежит версия, уведомляющая всех владельцев. Оставить одну.
- **Дыра 404 в боте**: `run_single_broadcasts.py:509` по-прежнему делает голый
  `continue` вместо пометки айтема ошибкой. Со стороны API случай закрыт, но
  бот провал не помечает.
- **Портрет площадки при постоянном отказе Telegram повторяется вечно**:
  ветка `permanent_error` есть только в событийной доставке.
- **`kudab-bot-cron` не входит ни в одну цель деплоя бота** в `Makefile`, хотя
  делит с ботом образ — после деплоя доставка крутит старый слой, и это ничем
  не видно.
- **`bot-release` зовёт `webhook-refresh`**, а прод работает в polling —
  вебхук ломает `getUpdates`. Перед правкой один раз посмотреть `make webhook-info`.
- **Удалить `kudab-publisher`** — мёртвая заготовка, к рассылке отношения не имеет.

---

## Как проверять

Отправку на стенде проверить **нельзя** без подготовки, и упирается это в два
независимых заслона: dev-бот `@dev_kudasobratbot` не админ в боевых каналах, а
`KUDAB_DEV_BROADCAST_CHAT_IDS` пуст при «по умолчанию запрещено».

Чтобы проверить вживую: создать **свой** приватный канал → добавить туда
dev-бота администратором с правом публикации → привязать канал в админке →
вписать его `chat_id` в `KUDAB_DEV_BROADCAST_CHAT_IDS` в `kudab-infra/.env` →
пересоздать `kudab-api` (планировщику переменная прокидывается отдельно, у него
нет `env_file`). **Никогда не вписывать туда id боевых каналов.**

Привязку, пикер и всё, что не доходит до отправки, проверять на стенде можно:
`BroadcastSafety` ограничивает публикацию, а не привязку. Результат смотреть по
базе и ответам ручек, **не по логам бота**: в нём не настроен logging, `log.info`
не печатается.

---

## Вопросы, на которые нужен ответ владельца

1. ~~Владелец канала — запретить или системный из env?~~ **Решено: запретить
   включать канал без телеграм-владельца.** Значит подключение через бота —
   основной путь, а ручная форма — черновик.
2. ~~Сколько слотов и в какие часы?~~ **Решено: два, 10:00 и 19:00 МСК.**
3. ~~Минимальный зазор между постами.~~ **Решено: 90 минут, только вместе с
   отсечкой просрочки в 2 часа.**
4. **Город при подключении канала** — спрашивать сразу в форме привязки или
   разрешать привязку без города и подсвечивать это проблемой?
5. ~~Портреты площадок — день в сетке или вне расписания?~~ **Решено: день,**
   назначается при постановке; гейт доставки перестаёт смотреть на тип записи.
   Подробности — очередь 3б.
6. **Нужен ли тестовый телеграм-канал** для живой проверки отправки — создавать
   его может только владелец.
7. ~~Портретов площадок в неделю?~~ **Решено: один.**
8. ~~Рубрики в вечернем слоте?~~ **Решено: раскладка принята** — пн подборка,
   чт портрет, остальное события. Подборка всё ещё требует связи «пост →
   события», без которой анти-дубли её не видят: делать её последней.
9. ~~Сколько подписчиков в канале?~~ **Снято:** встроенную статистику не ждём.

Открытыми остаются вопросы 4 и 6, плюс один из `CADENCE.md` — про 17 площадок
без ближайшего события (действует умолчание: в конец очереди с пометкой).
